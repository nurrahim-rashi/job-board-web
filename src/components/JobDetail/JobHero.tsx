import { ArrowRight, Bookmark, Calendar, Clock, MapPin, Share, Wallet } from "../site/Icons";
import { Stars } from "../site/Stars";
import { job } from "./jobData";
type JobHeroProps = { applied: boolean; saved: boolean; onApply: () => void; onSave: () => void };
export function JobHero({ applied, saved, onApply, onSave }: JobHeroProps) { return <section className="job-hero"><div className="night-sky" /><Stars /><div><p className="eyebrow light">Design · Senior · {job.type}</p><section><b>{job.company[0]}</b><span><h1>{job.title}</h1><p>{job.company} · {job.industry} · {job.city}</p></span></section><aside><button className="button button-light" disabled={applied} onClick={onApply}>{applied ? "Application submitted" : "Apply now"}<ArrowRight /></button><button onClick={onSave}><Bookmark />{saved ? "Saved" : "Save role"}</button><button onClick={() => navigator.share?.({ title: job.title, url: location.href })}><Share />Share</button></aside><dl><div><dt><Wallet />Salary</dt><dd>{job.salary}</dd></div><div><dt><MapPin />Location</dt><dd>{job.city}</dd></div><div><dt><Clock />Posted</dt><dd>{job.posted}</dd></div><div><dt><Calendar />Deadline</dt><dd>{job.deadline}</dd></div></dl></div></section>; }
